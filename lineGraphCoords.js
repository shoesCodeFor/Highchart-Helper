/* Line Graph Coordinates
 * Injectable function for getting the plot points of an SVG line for Highcharts
 *
 */

const centerOfLine =  (lineNumber = 0) => {
    let x = 0;
    let y = 0;

    return `${x_coord} ${y_coord}`;
};